function convertTimeToStr(time){
  console.log(time)
  let m = "0" + Math.floor(time / 60);
  let s = time % 60;
  s = s < 10 ? "0" + s : s + "";
  return m + ":" + s;
}

module.exports = {
  convertTimeToStr: convertTimeToStr
};